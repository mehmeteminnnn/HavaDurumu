import 'package:flutter/material.dart';
import 'package:flutter_animated_loadingkit/flutter_animated_loadingkit.dart';

var loader = AnimatedLoadingSpinner(
  circleColor: Colors.green,
  speed: Duration(milliseconds: 600),
);
